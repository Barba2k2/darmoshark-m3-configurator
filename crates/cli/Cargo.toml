[package]
name = "darmoshark-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.6", features = ["derive"] }
darmoshark = { version = "0.1.0", path = "../darmoshark" }

[[bin]]
name = "dms"
path = "src/main.rs"

[dev-dependencies]
serde_json = "1.0.151"
