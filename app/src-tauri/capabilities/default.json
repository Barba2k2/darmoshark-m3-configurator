{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The configurator window may invoke its own commands only.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
